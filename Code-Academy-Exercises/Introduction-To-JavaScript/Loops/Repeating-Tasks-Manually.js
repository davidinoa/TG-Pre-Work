const vacationSpots = ['Barcelona', 'Denver', 'London'];
console.log(vacationSpots[0]);
console.log(vacationSpots[1]);
console.log(vacationSpots[2]);

// Loops allow you to avoid doing repeating-tasks manually.
