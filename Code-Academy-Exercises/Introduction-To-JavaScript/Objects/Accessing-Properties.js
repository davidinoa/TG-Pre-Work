let spaceship = {
  homePlanet: 'Earth',
  color: 'silver',
  'Fuel Type': 'Turbo Fuel',
  numCrew: 5,
  flightPath: ['Venus', 'Mars', 'Saturn'],
};

// Write your code below
const crewCount = spaceship.numCrew;
const planetArray = spaceship.flightPath;

// To access a property with dot notation, we write the object's name, followed by the dot operator and then the property name.
